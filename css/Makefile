# This Makefile was generated by the made script:
# https://github.com/alx741/made
#


INPUTS = $(shell find . -maxdepth 1 -type f -name '*.sass')
OUTPUTS = $(foreach input, $(INPUTS), $(input:.sass=.css))

all: $(OUTPUTS)

%.css: %.sass palette.css
	sassc $< $@

clean:
	rm $(OUTPUTS)
